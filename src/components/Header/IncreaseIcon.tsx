import React from "react";
import Image from "next/image";

const IncreaseIcon = () => {
  return <Image src={"/assets/IncreaseQuantity.svg"} width={18} height={18} alt="increase" />;
};

export default IncreaseIcon;

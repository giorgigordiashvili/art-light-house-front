import React from "react";
import Image from "next/image";

const ProductImage = () => {
  return <Image src={"/assets/emptyImage.svg"} width={54} height={54} alt="product" />;
};

export default ProductImage;

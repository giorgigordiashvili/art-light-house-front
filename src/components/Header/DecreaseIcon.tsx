import React from "react";
import Image from "next/image";

const DecreaseIcon = () => {
  return <Image src={"/assets/DecreaseQuantity.svg"} width={18} height={18} alt="decrease" />;
};

export default DecreaseIcon;
